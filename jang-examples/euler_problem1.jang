fn solve(n: i64, acc: i64): i64 {
  if n == 1000 { ret acc }
  if n % 3 == 0 { ret solve(n + 1, acc + n) }
  if n % 5 == 0 { ret solve(n + 1, acc + n) }
  ret solve(n + 1, acc)
}

fn main(): i64 {
  ret solve(0, 0)
}
